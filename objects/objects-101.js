// Challage Area

// name, age, location
// Annes is 19 and lives in Tarutung. 
// Increase age by 1 and print message again

let myProfile = {

    name: 'Yohanes',
    age: 20,
    address: 'Tarutung'
}

console.log(`${myProfile.name} is ${myProfile.age} and lives in ${myProfile.address}`);

myProfile.age = myProfile.age+1;

console.log(`${myProfile.name} is ${myProfile.age} and lives in ${myProfile.address}`);